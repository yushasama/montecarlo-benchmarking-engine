apiVersion: 1
datasources:
  - name: ClickHouse
    type: clickhouse
    access: proxy
    url: http://localhost:8123  # Replace with your actual ClickHouse server URL
    jsonData:
      timeColumn: timestamp